# ── BlueByte AI — Master Dependencies ──────────────────────────────────
# Install: pip install -r requirements.txt

# ── Core Backend ──
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# ── WebSocket & Async ──
websockets>=12.0

# ── ZeroMQ Streaming ──
pyzmq>=25.1.0

# ── Database (SQLite async) ──
aiosqlite>=0.19.0

# ── Data Processing ──
numpy>=1.24.0

# ── Optional: ML / GNN (uncomment if using full GNN mode) ──
# torch>=2.0.0
# torch-geometric>=2.4.0
